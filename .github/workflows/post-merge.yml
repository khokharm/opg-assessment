name: Post-Merge Actions

on:
  push:
    branches:
      - main
      - develop

jobs:
  post-merge:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Post-merge message
        run: echo "Running post-merge actions..."
